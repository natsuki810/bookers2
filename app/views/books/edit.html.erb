<h2>編集画面</h2>
<%= form_with model: @book do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %>

<%= f.submit 'Update Book' %>

<%= link_to "Show", book_path %>
<%= link_to "Back", books_path %>

<% end %>